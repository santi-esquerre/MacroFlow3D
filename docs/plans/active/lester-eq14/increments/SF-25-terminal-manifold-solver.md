# SF-25 — Manifold-robust terminal solver

- State: `active`
- Goal: `Implementar el solver terminal robusto a la variedad de gauge en eta=1: Newton desplazado (mu*A + J) con schedule Levenberg-Marquardt y contingencia pseudo-transitoria, activado por el diagnostico D-gate.`
- Depends on: `SF-24`
- Unlocks: `SF-26`
- Branch: `science/lester-sf25-terminal-manifold-solver`
- Worktree: `Claude-managed per-node isolated worktrees`
- Acceptance gate: `Gate 1 + Gate 2 + Gate 3A`
- Human review: `required`
- Owner: `Claude Code orchestrator (Fable) + delegated workers`
- Started: `2026-08-14`
- Completed: `not completed`
- PR: `not opened`
- Commit: `not recorded`

## Scientific or engineering intent

Cross the `eta = 1` plateau that defeats both the damped Picard/Anderson
map (SF-21: non-contractive for `sigma_Y^2 >= 1` beyond `lambda ~ 0.5`)
and pure Newton-Krylov (SF-26 Gate A forensics: every inner GMRES solve
budget-exhausts; a 10x-budget probe did not complete). Working hypothesis
(falsifiable, tested FIRST by this increment's D-gate): at `eta = 1` the
Clebsch pair's recombination gauge freedom produces a solution MANIFOLD
whose tangent space renders the Jacobian near-singular in a large mode
cluster; the shift `(mu*A_blk + J)` moves that cluster to `~mu` in the
preconditioned spectrum, restoring Krylov convergence with the accepted
SF-23 preconditioner untouched. Full analysis, method survey, and
references: `docs/decisions/2026-08-14-manifold-robust-terminal-solver.md`.

## Preconditions

- SF-24 `done` (Newton phase, guard chain, rescue protocol — the terminal
  schedule extends it and recovers it bitwise at `theta = 0`).
- The SF-26-carried wiring (per-stage attribution counters, smoke Newton
  wiring, five-config enablement) merged with the restructuring PR — the
  D-gate's frozen-plateau protocol and forensics depend on it.

## In scope

- D-gate diagnostic (frozen plateau state; mu-sweep; spectral probe; LM
  mini-solve) BEFORE any solver integration.
- `ShiftedJacobianOperator` (apply = Jv + mu*A per component) and the
  duck-typed operator generalization of `CoupledGmres::solve` (defaulted,
  zero impact on accepted call sites).
- Terminal schedule in the Newton phase: LM `mu_k = theta * r_F_k`
  (primary; theta calibrated by the D-gate), config surface, reports,
  memory accounting; `theta = 0` bitwise-recovers SF-24.
- SER/Ψtc schedule as CONTINGENCY ONLY (triggered iff the LM mini-solve
  fails while the mu-sweep confirms the mechanism), with its own recorded
  decision before implementation.
- Acceptance demonstration: a NEW test fixture (not the SF-26-owned smoke
  case) running the sigma^2=1 32^3 continuation with the terminal
  schedule enabled to `lambda = 1`.

## Out of scope

- The SF-26 moved-verbatim gates themselves (re-imposed by SF-26 with the
  sanctioned wiring change there).
- Gauge-fixed/bordered reformulations (the recorded next research
  direction if the D-gate falsifies the shift mechanism).
- Explicit pseudo-time; mixed precision; grids beyond 32^3 fixtures;
  preconditioner changes.

## Files and symbols

- Add `src/physics/streamfunctions/ShiftedJacobianOperator.cuh/.cu` (or
  colocated in `NewtonKrylovSolver.*` if cleaner).
- Extend `CoupledGmres::solve` with a defaulted duck-typed operator
  template parameter (accepted call sites unchanged).
- Extend `NewtonKrylovConfig` with the `terminal` sub-config
  (`theta`, `mu_min`, `mu_max`, mode; defaults preserving SF-24 bitwise).
- New `tests/streamfunctions/terminal_solver_gpu_cases.cu` + registry +
  ctest entries (cheap diagnostic-unit cases + heavy demonstration).

## Implementation specification

1. D-GATE FIRST (heavy diagnostic case, orchestrator-run on the V100):
   freeze the plateau state deterministically (warm-start `lambda=0.5125`
   from the accepted `(0.5, 1)` sigma^2=1 smoke state; run the accepted
   Picard/Anderson stage to its stagnation exit); then (a) mu-sweep
   `mu in {1e-1, 3e-2, 1e-2, 3e-3, 1e-3, 3e-4, 1e-4, 0}` with the
   accepted GMRES (restart 10, budget 100), recording per-mu
   iterations/achieved reduction; (b) shifted-power spectral probe of
   `M^-1 J`'s smallest modes; (c) LM mini-solve with theta calibrated
   from (a).
2. Only after the D-gate passes: the operator wrapper, the GMRES
   generalization, the LM schedule inside the accepted Newton phase
   (rescue/retry protocol and trial guard chain UNCHANGED; Armijo stays).
3. Shifted-apply exactness is unit-gated against an independent
   recompute; `theta = 0` bitwise equivalence with SF-24 is gated.
4. The demonstration fixture runs the full sigma^2=1 32^3 continuation
   with terminal enabled.

## Expected numerical effect

At the plateau, GMRES on the shifted system converges in O(10) iterations
instead of budget-exhausting; LM steps descend superlinearly onto the
solution manifold (any manifold point is physically valid — every
acceptance metric is gauge-invariant); the sigma^2=1 continuation reaches
`lambda = 1` with per-stage `r_F <= 1e-6`.

## Validation commands

```bash
cmake --build build/wsl-debug -j
ctest --test-dir build/v100-release --output-on-failure -R streamfunction_terminal
./build/v100-release/streamfunction_operator_tests --case terminal_dgate_diagnostic
./build/v100-release/streamfunction_operator_tests --case terminal_sigma1_demonstration
```

(Validation executes on the remote V100 per the standing venue policy;
workers are compile-gate only.)

## Acceptance thresholds

- D-gate: some `mu > 0` restores GMRES convergence with `>= 10x` fewer
  iterations than `mu = 0` on the frozen plateau system; the LM
  mini-solve reaches `r_F <= 1e-4` (10x below the observed plateau floor)
  within its recorded budget. FAILURE of the mu-sweep = the hypothesis is
  falsified: report BLOCKED with the spectral evidence (no downstream
  gate may be weakened).
- Shifted-apply exactness to roundoff; `theta = 0` bitwise == SF-24 path;
  determinism; exact memory accounting (zero when disabled).
- Demonstration: sigma^2=1 32^3 continuation with terminal enabled
  reaches `lambda = 1`, every accepted stage `r_F <= 1e-6`.
- No regression: full suite green except the known SF-26-owned smoke
  entry; default-pipeline byte-compares clean.

## Regression surface

- Accepted SF-24 Newton behavior (theta = 0 bitwise), CoupledGmres
  call-site compatibility, Newton-phase memory accounting, continuation
  interaction, suite runtimes.

## Failure and rollback policy

- The D-gate decides before solver work is written; a falsified mechanism
  is a structured BLOCKED outcome, not a tuning license.
- The SER contingency requires its own recorded decision (non-monotone
  acceptance interacts with the guard chain) BEFORE implementation.
- No SF-26 gate, seed, tolerance, budget, or fixture value may change.

## Completion checklist

<!-- completion-checklist:start -->
- [ ] D-gate diagnostic executed and passed (or BLOCKED honestly reported).
- [ ] Shifted operator + GMRES generalization implemented and unit-gated.
- [ ] LM terminal schedule integrated; theta=0 bitwise equivalence gated.
- [ ] sigma^2=1 32^3 demonstration reaches lambda=1 (r_F<=1e-6 per stage).
- [ ] Gate 3A regressions and human review pass.
- [ ] Evidence, PR, and commit are recorded.
- [ ] Dashboard marks SF-25 complete and selects SF-26.
<!-- completion-checklist:end -->

## Advancement rule

SF-26 re-imposes the moved-verbatim heterogeneity gates with the terminal
schedule as its sanctioned wiring change once SF-25 is accepted and merged.

## Bitácora

| UTC | Commit/state | Observation or action | Evidence/decision | Next action |
|---|---|---|---|---|
| 2026-08-14T13:30Z | created by the owner-directed option (B) restructuring | This increment exists because the accepted SF-22..24 Newton-Krylov stack, correctly wired (SF-26 Gate A evidence), cannot cross the `eta=1` plateau for `sigma_Y^2>=1`: per-stage attribution showed every inner GMRES budget-exhausting (~112 Jv/accepted step) and a prespecified 10x-budget probe timed out without completing. The research record (`docs/decisions/2026-08-14-manifold-robust-terminal-solver.md`) derives the gauge-manifold hypothesis (Clebsch recombination freedom => near-singular Jacobian cluster exactly at eta=1), surveys Ψtc (Kelley-Keyes) and inexact LM under local error bounds (Dan-Yamashita-Fukushima) — both reducing to the SAME shifted system `(mu*A+J)delta=-F` whose A-metric shift restores the preconditioned spectrum with the SF-23 preconditioner untouched — and fixes the diagnostic-first plan: the falsifiable D-gate decides before any solver code. | Gates prespecified in this file; the mu-sweep grid, frozen-state protocol, and thresholds are bound BEFORE any run. | Activate only when named by `NEXT` after the restructuring PR is merged. |
| 2026-08-14T15:20Z | activation on `master=fc7a48b` (restructure merged; checker OK next=SF-25) | PRESPECIFIED protocol refinements, bound BEFORE any implementation or run (decisions E1-E5 of this increment): **E1 (D-gate enabler reading):** the mu-sweep requires the shifted-apply MECHANISM, so the duck-typed operator parameter on `CoupledGmres::solve` (defaulted to `JvpWorkspace`, all accepted call sites unchanged) and the standalone `ShiftedJacobianOperator` module are D-gate ENABLERS shipped before the diagnostic; what remains gated behind the D-gate is the TERMINAL SCHEDULE integration into the Newton phase (the behavioral solver change). **E2 (freeze protocol):** the plateau state is frozen with newton DISABLED (the SF-21-characterized regime, and ~4x cheaper): run the sigma^2=1 32^3 smoke continuation (seed 12345, ell=8, epsilon 1e-2, Anderson R5 config) to its lambda floor (~1580 s, deterministic), take the accepted `(lambda=0.5, eta=1)` state, warm-start the `lambda=0.5125` problem, run the accepted Picard/Anderson stage to its stagnation exit; that state is THE frozen plateau instance. **E3 (mu-sweep):** solve `(mu*A_blk + J)delta = -F` at `mu in {1e-1, 3e-2, 1e-2, 3e-3, 1e-3, 3e-4, 1e-4, 0}` with the accepted GMRES (restart 10, budget 100, rel_tol = sqrt(r_F_frozen) clamped to [1e-8,1e-1] — the SF-24 forcing value at the frozen state, so the sweep answers exactly the question the Newton phase asks); record per-mu status/inner iterations/true-residual reduction. MECHANISM CONFIRMED iff some mu>0 converges with >=10x fewer iterations than mu=0's budget exhaustion. **E4 (spectral probe):** generalized inverse iteration toward the smallest (J, A_blk) eigenpair using the shifted solve itself (v_{k+1} = normalize(solve(mu*A+J, A_blk v_k)) at mu=1e-3, 20 iterations), reporting the generalized Rayleigh quotient trajectory <v,Jv>/<v,A v>; hypothesis-supporting if |lambda_min^gen| <= 1e-2 (vs O(1) regular modes). **E5 (LM mini-solve + theta rule):** theta = mu*/r_F_frozen with mu* = the SMALLEST swept mu that converged within budget; bounded shifted-Newton loop (max 30 steps, Armijo + the identical guard chain, test-local driver) must reach r_F <= 1e-4. D-GATE VERDICT: E3 fails => hypothesis falsified => BLOCKED with the E4 evidence; E3 passes but E5 fails => SER contingency decision point (own recorded decision); both pass => proceed to the terminal-schedule integration. Venue: workers compile-gate only; every run orchestrator-driven on the V100. | DAG: T01 enablers (CoupledGmres operator param + ShiftedJacobianOperator) -> T02 diagnostic test case -> D-GATE (orchestrator, V100) -> [T03 terminal schedule + T04 tests + T05 io surface] -> I01 -> F01 (incl. sigma^2=1 demonstration). | Delegate T01; regenerate base refs at fc7a48b. |
| 2026-08-14T12:10Z | D-GATE FIRST RUN on chain `d11f63a->4e22528->65e7a6f->9985f90` (V100 job `sf25-dgate`; raw log `/tmp/dg_main.log`; unit case PASS with max_diff=0 and bitwise mu=0 passthrough) | **E2 freeze:** reproduced exactly (lambda_floor_exhausted, 0.5, eta=1, 83 stages); the lambda=0.5125 stage froze at r_F=1.1205e-3 (in the prespecified band) but exited `omega_floor_rejected` (k=20) rather than `stagnated` — BOTH are documented SF-21 plateau signatures (the ramp attempts stagnated; the direct lambda attempt hits the omega floor first: every Picard trial rejected). AMENDMENT E6a (methodology, recorded pre-corrective): the E2 exit-reason assert accepts {stagnated, omega_floor_rejected}. **E3 — MECHANISM CONFIRMED:** mu=0.1 -> 5 inner iterations (20x under the mu=0 budget exhaustion), mu=0.03 -> 12, mu=0.01 -> 38, mu<=0.003 -> budget-exhausted; gate PASS. **E4 — the near-null cluster is real and SIGNED:** generalized Rayleigh converges to lambda_min^gen = -2.059e-3 — ~500x below the O(1) regular modes AND NEGATIVE (J indefinite at the plateau); quantitative cross-consistency: the E3 convergence cliff sits exactly where mu crosses |lambda_min| (shifted mode mu+lambda crosses zero at mu~2e-3). **E5 — FAILED at k=0, informatively:** theta=8.92, GMRES converged (38 its), but EVERY alpha in the ladder was Armijo-rejected with r_F_trial approaching r_F_k from above (directional slope ~0+ at alpha->0). Combined with the freeze's omega-floor (Picard trials all rejected too), the evidence identifies the plateau as a SPURIOUS LOCAL MINIMUM of the merit 0.5*||F||^2 (J^T F ~ 0 with F != 0, J indefinite) — precisely the case the Kelley-Keyes Psi-tc theory addresses ("line-search globalization stagnates at local minima; pseudo-transient continuation succeeds") and the case NO monotone-Armijo method can escape. Per the PRESPECIFIED decision tree (activation E5 rule): E3-pass + E5-fail => the SER contingency decision point. **DECISION E6 (recorded HERE, before the corrective run):** extend the diagnostic with a Psi-tc/backward-Euler probe at the SAME frozen state: loop k<=60: evaluate r_F (PASS at <=1e-4); mu_k = 1/dtau_k; solve (mu_k*A+J)delta=-F (same GMRES config, rel_tol clamp(sqrt(r_F_k),1e-8,1e-1)); candidate = P(state+delta) accepted WITHOUT Armijo but under STRICT bounded-non-monotone safeguards: nonfinite OR r_F_new > 2*r_F_k => reject, dtau/2, retry (<=3 per step, else FAIL); global divergence stop r_F > 10*r_F_frozen => FAIL; SER update dtau_{k+1} = clamp(dtau_k * r_F_{k-1}/r_F_k, 1, 1e6); dtau_0 = 1/mu_star = 100 (E3-calibrated). The D-gate verdict rule per the prespecified tree becomes: PASS iff E2 (amended) AND E3 AND (E5 OR E6). The probe is falsifiable: the flow's LINEARIZATION at the frozen state is weakly unstable along the -2e-3 mode, so E6 tests precisely whether the nonlinear flow traverses the ridge — if it also fails, the increment reports the structured evidence and the gauge-fixed/bordered formulation becomes the recorded direction. Scope note: E6 is a DIAGNOSTIC (test-local); any production non-monotone acceptance remains a separate design surface for the PR review. | Raw numbers retained above and in the remote log; the E4 trajectory (20 values) converges to 5 significant figures. | Corrective C02: E6a assert amendment + the E6 probe section; rerun the diagnostic. |
| 2026-08-14T13:05Z | D-GATE SECOND RUN on chain `...->a440824` (V100 job `sf25-dgate2`) — E6 Psi-tc probe FAILED, and the combined evidence now identifies the mechanism | E2 (amended) PASS deterministically; E3 PASS (identical numbers); E4 identical (lambda_min=-2.059e-3). **E6: monotone ASCENT under the flow** — every accepted backward-Euler step increased r_F (1.12 -> 1.39 -> 1.95 -> 2.82 -> 3.98 -> 5.43 -> 7.17 -> 9.15 -> 11.5e-3; divergence stop at k=8), with SER driving mu upward 0.01 -> 0.33 and the k=0/dtau=100 candidate EXACTLY matching E5's alpha=1 trial (2.5970e-3 — same shifted system, internal consistency). Every sampled method family now agrees the frozen state is a GENUINE local minimum of the merit with F != 0: Picard (omega floor), Anderson, Newton-Armijo (E5: all alphas rejected, slope 0+), LM at any swept mu, and the Psi-tc flow (ascent across mu in [0.01, 0.33]). **The decisive clue:** SF-21/D-gate stage data show eta=0.996875 stages converging to r_F ~ 1.2e-7 at the SAME lambda/epsilon — a 0.3% change in eta moves the attainable residual by FOUR DECADES. That is the classic signature of a FOLD (saddle-node) of the solution branch in eta: eta_fold(lambda, sigma^2, epsilon) crosses 1 near lambda ~ 0.5 for sigma^2 = 1 at epsilon = 1e-2; beyond it the epsilon-REGULARIZED eta=1 system has NO solution in this branch and the plateau r_F ~ 1e-3 is the least-squares distance-to-solvability (exactly J^T F = 0, F != 0, indefinite J — all measured). The paper's own solve uses NO epsilon regularization, so the fold may be an artifact of epsilon = 1e-2 and move past eta = 1 at smaller epsilon — which would ALSO explain sigma^2 = 0.25 (fold far above 1) and the lambda boundary (fold position crossing 1). **PRESPECIFIED PROBES (E7/E6b, recorded HERE before the corrective run; print-only evidence, no verdict change — the D-gate verdict stands FAIL for the terminal-method demonstration):** E7 epsilon-fold probe at the SAME frozen state: (i) one residual evaluation of the frozen state under epsilon = 1e-3 (starting point); (ii) ONE warm-started stage solve at eta = 1 with config.epsilon = 1e-3 (everything else identical, picard.max_iter = 200 bound); (iii) if (ii) stalls, the E5-style LM mini-solve at epsilon = 1e-3 (30 steps). DECISIVE fold evidence iff the reached r_F < 1e-5 (>=100x below the epsilon=1e-2 plateau); supportive in [1e-5, 1e-4]; refuting if it stalls ~1e-3 again (then the obstruction is epsilon-robust and intrinsic to eta = 1). E6b micro-step scan (closes the step-size loophole): single backward-Euler candidates at mu in {1, 10} from the frozen state, printing the r_F deltas — if even near-infinitesimal flow steps ascend, F^T J A^-1 F < 0 is established directly. | The increment's scoped Goal (shifted-Newton terminal solver) is NOT demonstrable at this state per the D-gate; the evidence will be escalated to the owner with the fold dossier after E7/E6b — the resolution surface (epsilon-aware stage protocol / pseudo-arclength continuation in eta / regularization redesign) exceeds SF-25's scope and touches SF-21-prespecified protocol ordering. | Corrective C03 (E7+E6b, print-only) -> rerun -> BLOCKED escalation with the complete dossier. |
| 2026-08-14T14:00Z | D-GATE THIRD RUN (`sf25-dgate3`): E6b + E7 evidence in; one hypothesis killed, one final discriminating probe prespecified | **E6b (micro-step scan):** near-infinitesimal backward-Euler steps DESCEND — delta_rF = -3.58e-8 at mu=1 and -1.77e-8 at mu=10 (GMRES 1 inner iteration each). The frozen state is therefore NOT a strict local minimum but a NEAR-STATIONARY SHELF: the flow descends at ~3e-5 RELATIVE per unit step while every practical step size (mu <= 0.33, full steps) overshoots into ascent — escaping along the flow would need O(1e4-1e5) implicit steps (~40 residual evals each). Notably this is consistent with the paper's own method profile: massive counts of CHEAP explicit pseudo-time steps. **E7 (epsilon-fold probe): REFUTING** — at epsilon=1e-3 the picture is unchanged (frozen-state r_F 1.286e-3; stage stalls at omega floor r_F=1.260e-3 in 8 iterations; the LM mini-solve rejects every alpha at k=0 with the same approach-from-above signature). The obstruction is EPSILON-ROBUST: not a regularization artifact. The fold-position-in-epsilon hypothesis is dead; a fold in eta with epsilon-insensitive position (floors 1.12e-3 vs 1.26e-3, nearly equal) remains viable. **THE REMAINING DISCRIMINATION — branch vs problem:** every stalled path shares one property: it is WARM-STARTED along the lambda-continuation branch. The paper NEVER continues: it initializes with the HARMONIC (zero-source) solve at FULL heterogeneity and pseudo-time-steps from there — a potentially different basin. SF-21's own baseline (lambda=0, zero_source) and the anderson_stall fixtures (lambda*=0.1125, zero_source) converge; a DIRECT zero-source solve at lambda~0.5, sigma^2=1 has never been tried. **PRESPECIFIED PROBE E8 (the LAST probe before escalation, print-only, recorded HERE before the corrective run):** at the E2 attempt setup (lambda=0.5125 problem, same Y_att/flow/gauge), ONE `solve_streamfunctions` with `initial_state = zero_source`, `coefficient_state = rebuild`, fresh fields, eta=1, epsilon=1e-2, anderson R5 on, newton off, `picard.max_iter = 500`; print status/exit_reason/iterations/final r_F (+ first/last picard_history r_F values). DECISIVE (branch-fold) iff converged r_F <= 1e-6: the plateau is then an artifact of the WARM-STARTED continuation branch and the actionable resolution is a stage-solver INITIALIZATION POLICY (attempt harmonic init when the warm-started stage stalls) — small, protocol-level, owner-reviewable. REFUTING iff it stalls ~1e-3: the obstruction is init-independent and intrinsic to eta=1 at these parameters; full BLOCKED escalation with the gauge/topology lead. Either outcome ESCALATES immediately after this run — no further probes. | E6b/E7 raw numbers above; all deterministic reruns of E2-E6 identical to the second run. | Corrective C04 (E8 print-only) -> rerun -> escalation with the complete dossier, whichever branch E8 selects. |
| 2026-08-14T14:40Z | D-GATE FOURTH RUN (`sf25-dgate4`): E8 REFUTING_INTRINSIC — the dossier is complete; STRUCTURED BLOCKED ESCALATION to the owner | **E8 (harmonic/paper-style init, zero_source, fresh workspace, lambda=0.5125, sigma^2=1, eta=1, eps=1e-2, anderson R5):** converged from r_F=0.2525 down to **8.3045e-4 and STAGNATED at k=77** (anderson_acc=48, rej=12) — the SAME ~1e-3 shelf from a fully independent basin. The obstruction is now measured to be: INIT-independent (warm-started branch AND harmonic init), EPSILON-robust (1e-2, 1e-3), METHOD-independent (Picard/omega-floor, Anderson, Newton-Armijo, LM at every swept mu, Psi-tc flow ascent at practical steps + infinitesimal descent ~3e-8/step), KRYLOV-budget-independent (10x probe), with measured spectral structure (lambda_min^gen = -2.059e-3, indefinite J) and the four-decade eta-cliff (0.996875 -> 1.2e-7; 1.0 -> shelf). **AMPLITUDE IDENTITY (pure arithmetic on existing data, no new run):** with normalize_variance=true, Y(lambda=0.5125, sigma^2=1) = 0.5125*Y_unit and Y(lambda=1, sigma^2=0.25) = 0.500*Y_unit are the SAME FIELD up to a 2.5% amplitude factor — one converges to 1e-7 (SF-21/SF-26 evidence), the other shelves at 1e-3 (this dossier). The eta=1 attainability boundary is therefore a CRITICAL AMPLITUDE a* in (0.500, 0.5125]*Y_unit at THIS resolution (ell/h=8), and SF-21's sigma^2=0.25 success is not a counterexample (it lives entirely below a*). **REMAINING HYPOTHESIS (strongest, untested):** a* is a DISCRETIZATION limit of the eta=1 system at ell/h=8 — the paper solved amplitude 2 (sigma^2=4) at ell/h=16 (256^3, ell=1/16), twice our resolution per correlation length; if a* grows with ell/h, every sigma^2>=1 gate at ell/h=8 (the 32^3 smoke AND the whole 64^3 suite) is infeasible BY CONSTRUCTION for any solver, and the correct road to the paper's case is RESOLUTION-PER-CORRELATION-LENGTH (the plan's grid-continuation axis), not terminal-solver heroics at ell/h=8. Discriminating experiment (PROPOSED for owner approval, not run): the same continuous realization at ell/h=16 with L/ell=4 (64^3, ell=16h — the 32^3 fixture's domain ratio at doubled resolution): measure whether a* moves. **STATUS: the SF-25 Goal (terminal solver) is NOT demonstrable per the D-gate — both prespecified method branches failed at a state now understood as likely BEYOND the attainability boundary of the discrete problem; no solver can fix nonexistence. Escalated BLOCKED to the owner with the resolution-surface options (gate revisit at ell/h>=16 / pseudo-arclength branch tracing / gauge-topology research). No gate value was changed at any point; the sigma^2=0.25 improvement, the D-gate enablers (shifted operator + GMRES generalization, unit-PASSED with max_diff=0), and the complete forensic instrumentation stand as auditable deliverables on this branch.** | Every probe prespecified before its run (rows above); all raw numbers versioned; deterministic reruns E2-E7 bitwise-consistent across four runs. | AWAIT OWNER DECISION on the resolution surface; no further probes. |
| 2026-08-14T15:20Z | OWNER DECISION: option (A) — the resolution-discriminating experiment. PRESPECIFIED design (recorded HERE before any run) | **Experiment R1 (owner-approved, two direct solves at the critical amplitude, no continuation):** both at sigma^2=1, seed 12345, normalize_variance, Y scaled by lambda=0.5125 (the amplitude 0.5125*Y_unit that shelves at 32^3), eta=1, epsilon=1e-2, zero_source init, coefficient rebuild, anderson R5 (depth 5, start 5, limit 1e12), newton disabled, adaptive defaults, picard.max_iter=500, SF-19 affine flow qbar=(1,0,0): **R1a** 64^3, dx=1, ell=16 (ell/h=16, L/ell=4 — the 32^3 fixture's domain ratio at DOUBLED resolution per correlation length, the paper's own ell/h). **R1b (control)** 64^3, dx=1, ell=8 (ell/h=8, L/ell=8 — the OLD resolution-per-ell at the new grid size, isolating ell/h from grid size and domain ratio; note this is exactly the existing 64^3 suite's resolution rule). PRESPECIFIED readouts: R1a CONFIRMING iff converged r_F<=1e-6 (a* moved past 0.5125 at ell/h=16); R1b SHELF-EXPECTED iff not_converged with final r_F in [1e-4,1e-2] (ell/h=8 shelves at BOTH L/ell=4 and 8 -> domain ratio ruled out). Joint verdicts: R1a-converge + R1b-shelf = RESOLUTION HYPOTHESIS CONFIRMED (ell/h is the controlling parameter; the sigma^2>=1 gates at ell/h=8 are infeasible by discretization; benchmark ladder must move to ell/h=16, owner-gated); R1a-shelf = hypothesis REFUTED at this step (escalate; gauge/topology or realization-dependence next); R1a-converge + R1b-converge = grid-size/domain effect, not ell/h (escalate with revised analysis). REALIZATION CAVEAT (recorded): the SF-18 generator hashes seed-to-INTEGER-mode, so the 64^3 realizations are statistically equivalent, dimensionlessly matched samples — not the bitwise-same continuous field as the 32^3 fixture; a* is in principle realization-dependent (the 2.5% margin at 32^3 suggests sensitivity), so a confirming R1 is evidence about THIS seed's family at doubled resolution, stated as such. Implementation: new HEAVY print-only experiment case `terminal_resolution_probe` (always-pass evidence recorder, verdict-evidence lines, full solver reports printed); wall bound 6 h. | Runtime estimate 1.5-4 h (two ~77-150-iteration 64^3 solves). After the run: experiment note in docs/experiments/ + the restructure proposal per the joint verdict. | Corrective C05 (the probe case) -> run `sf25-resprobe` -> report to owner with the restructure proposal. |
| 2026-08-14T15:55Z | R1 RUN (`sf25-resprobe`): mechanical verdict HYPOTHESIS_REFUTED_R1A_SHELVED — but the honest reading is a CONFOUNDED readout by an unanticipated third exit mode | Raw: R1a (ell/h=16): `omega_floor_rejected` at 21 iterations / **15.8 s**, r_F=9.075e-3, trajectory STILL DESCENDING healthily (0.263 -> 9.1e-3, anderson 7 acc / 5 rej). R1b (ell/h=8 control): `omega_floor_rejected` at 13 iterations / 10.3 s, r_F=2.639e-2 — **OUTSIDE the prespecified shelf band [1e-4,1e-2]**, so the r1b_shelf criterion could not classify it and the printed refuted-verdict rests solely on `!converged`. NEITHER run exhibits the 32^3 shelf phenomenology (77+ iterations, flattening at ~1e-3, infinitesimal descent): both died of PREMATURE ADAPTIVE-OMEGA COLLAPSE mid-descent at levels 9-26x above the 32^3 shelf — a globalization artifact the R1 readout criteria did not anticipate (they assumed converge-or-shelf). The 15-second 64^3 walls also retire the earlier 1.5-4 h cost estimate. **PRESPECIFIED DECONFOUNDING RUN R2 (recorded HERE before the corrective run):** the SAME two problems (64^3, amplitude 0.5125*Y_unit, sigma^2=1, seed 12345, ell=16 and ell=8, harmonic init, anderson R5, adaptive defaults untouched) with **`newton.enabled = true` (SF-24 defaults)** — the accepted stack AS DESIGNED, whose threshold activation (r_F<=1e-2) fires exactly where R1a's omega died (9.1e-3) and whose stagnation-redirect/rescue machinery is the designed answer to globalizer stalls. READOUTS (prespecified): R2a converged r_F<=1e-6 => the R1 refutation was the globalizer artifact and the RESOLUTION HYPOTHESIS IS CONFIRMED-VIA-STACK (a* moved past 0.5125 at ell/h=16 for the as-designed solver), simultaneously demonstrating the SF-24 stack completing at the paper's resolution rule; R2a not_converged with `newton_exhausted`/plateau signature at ~1e-3-class r_F => the wall exists at ell/h=16 too => resolution hypothesis GENUINELY refuted (escalate: gauge/topology or realization-dependence next; no further probes); R2b (control) informs whether ell/h=8 at 64^3 shows the wall when the full stack pushes below 1e-2. Implementation: extend the probe helper with a `bool enable_newton` parameter; run R1a/R1b (unchanged prints) then R2a/R2b; extend the joint-verdict line with the R2 rules above. | R1 raw numbers retained above and in `/tmp/rp_main.log`. No gate/threshold touched; adaptive defaults untouched (the deconfound uses the stack's own designed rescue path, not a tuning of omega_min). | Corrective C06 (R2 sub-runs) -> run -> the definitive resolution report to the owner. |
| 2026-08-14T16:20Z | R2 RUN (`sf25-resprobe2`): joint verdict **WALL_AT_ELLH16** — the prespecified no-further-probes escalation fires | Raw: **R2a** (ell/h=16, newton ON): `omega_floor_rejected` at k=12, r_F=8.772e-3, wall 146.5 s; newton_act=1, newton_acc=8, newton_fail=1, newton_rescue=0 — Newton activated at the threshold, accepted 8 slow steps (~16 s/step ~ 50-100 residual evals/step: the plain-Newton GMRES budget-exhaustion pattern from the 32^3 plateau, unshifted theta=0 stack), then one step failure, and the rescue window died at the omega floor before the retry. **R2b** (ell/h=8 control, newton ON): newton_act=0 (died at 2.64e-2, above the 1e-2 activation threshold), trajectory bitwise-identical to R1b. R1a/R1b reproduced bitwise. **Prespecified rule fired:** R2a not_converged with a plateau-signature exit (omega_floor, per E6a) at in-band r_F => WALL_AT_ELLH16 => escalate, NO FURTHER PROBES. **Recorded caveats for the owner (analysis, not new measurements):** (1) R2a again died IN THE STATE MACHINE (rescue-window Picard steps at a crashed persistent omega) while Newton was still slowly descending — the 64^3 wall is SUGGESTED but not shelf-PROVEN in the E6b sense (no micro-step flatness measurement at that state); the readout criteria conflated died-with-walled a second time. (2) DESIGN GAP made visible by the R2a trace: after a Newton step failure the rescue window INHERITS the crashed persistent omega (near floor) — five rescue steps at omega~floor are near-no-ops doomed to reject; rescue re-entry arguably should restart omega at its initial value (an SF-24 E6-protocol robustness defect candidate, actionable regardless of the resolution question). (3) The ONE method class never tested remains the paper's own: EXPLICIT pseudo-time (1 cheap residual eval per step, 1e4-1e6 steps) — E6b measured infinitesimal descent (-3.6e-8/step) on the 32^3 shelf, so brute-force explicit traversal is exactly what the paper's method would attempt where every implicit/monotone method stalls economically. **STATUS: escalated to the owner with options** (A2) paper-faithful explicit pseudo-time probe at the 32^3 frozen amplitude (cheap per step, wall-bounded, finally tests the paper's method class; decisive either way), plus the (B2) omega-rescue-reset robustness fix as its companion; (C2) close SF-25 BLOCKED-with-dossier and re-plan. No probe runs without owner direction. | All raw numbers above and in `/tmp/rp2_main.log`; R1/R2 fully deterministic (R1 lines bitwise-reproduced across runs). | AWAIT OWNER DECISION. |
| 2026-08-14T17:10Z | OWNER-DIRECTED REMEDIATION RESEARCH: exhaustive numerical-analysis literature investigation of the measured failure modes, constrained to saneamiento of the existing stack (no method replacement) | Deliverable: `docs/decisions/2026-08-14-terminal-shelf-remediation-options.md` — failure modes FM1-FM6 mapped to literature (GLL 1986 / Zhang-Hager 2004 / Li-Fukushima 2000 nonmonotone acceptance; Kelley-Keyes 1998 Psi-tc with V=A preconditioned flow and the SER-misuse diagnosis of E6; Transtrum-Machta-Sethna geodesic acceleration for canyon traversal; Keller 1977 + Ipsen et al. pseudo-arclength fold theory; homotopy-endgame geometric approach for the eta-cliff; Toth-Kelley / Pollock-Rebholz / Evans et al. Anderson restart-damping; nested-iteration basin enlargement), each remedy expressed on audited inventory items (F, A+MG, Jv, ShiftedJacobianOperator, templated CoupledGmres, SF-24 line search, SF-20 Anderson, SF-17/21 axis stepper with configurable min_step floors, MG transfers, SF-11 diagnostics, D-gate instrument). Option menu: **S1** eta-endgame (config: lower eta min_step, geometric 1-eta walk, optional extrapolated eta=1 init), **S2** state-machine hygiene (omega-rescue reset = ex-B2; omega-collapse guard on descending-slope floor rejections; Anderson restart-on-stagnation), **S3** terminal descent-flow saneamiento of the Newton phase (Li-Fukushima/GLL acceptance + descent-servoed mu_k on (mu*A+J) seeded by a prespecified single-step mu-scan over {0.3,0.4,0.5,0.7,1,2} on the frozen E2 state + large cheap step budget + optional geodesic correction + BLOCKED abort rule; the paper's pseudo-time class in implicit A-metric form, explicit variant retained as parity control), **S4** coarse-grid warm start (16^3 same-seed low-pass solve + MG prolongation; SF-27 essence as probe), **S5** pseudo-arclength lambda leg (fold-or-not diagnosis; only if S1-S3 leave it open), **S6** physical-quality measurement of the shelf/eta<1 pair (SF-11 + spatial defect localization; consumability + localized-obstruction test). Recommended phases: 1 = S2+S1+S3-microprobe+S6 (cheap, prespecified, no policy commitment); 2 = S3 full (gated on mu-scan, proven in the D-gate instrument before promotion); 3 = S4/S5 contingent. Honest boundary recorded: E6b descent proves the shelf is not a strict merit minimum, which is exactly the regime where 4a/4b literature applies. No implementation or runs authorized by this record. | AWAIT OWNER OPTION SELECTION |
| 2026-08-14T17:40Z | OWNER DECISION: **Phase 1 COMPLETE approved** (S2+S1+S3-microprobe+S6). PRESPECIFIED Phase-1 protocol recorded HERE before any implementation or run | **DAG:** P1-H (worker: S2 hygiene, compile-gate only) -> P1-I (worker: probe instrument, depends on P1-H commit, compile-gate only) -> orchestrator V100 session (all execution). **P1-H (S2), config-gated, ALL defaults OFF == behavior-identical, additive counters in `StreamfunctionSolverReport`:** (a) `newton.rescue_resets_omega{false}`: rescue-window entry resets persistent adaptive omega to solve-start initial (`picard.omega`); counter `newton_rescue_omega_resets`. (b) `adaptive.floor_guard{enabled=false, window=5, drop_factor=0.9, max_resets=3}`: an omega-floor rejection that would end the stage is intercepted IFF >= `window` accepted iterations exist since start/last reset AND `r_F[k] <= drop_factor * r_F[k-window]` over accepted history (still descending >=10% per 5 accepted); action: reset persistent omega to initial, `omega_floor_guard_resets`++, continue; beyond `max_resets` -> ordinary `omega_floor_rejected`. (c) `anderson.restart_on_stagnation{enabled=false, max_restarts=2}`: intercepts the `stagnated` exit while restarts remain: clear Anderson history + reset omega to initial + reset the stagnation detector window state; counter `anderson_stagnation_restarts`. Validation additions (window>=1, drop_factor in (0,1), max_resets>=0, max_restarts>=0). Acceptance: existing full suite passes UNCHANGED (defaults-off inertness at observable level); new validation tests; ON-path counter >0 demonstrated in a light fixture per feature where feasible (floor-guard ON-path fallback evidence allowed from arm S1b); YAML loader parsing EXCLUDED (programmatic-only in Phase 1); benchmark YAMLs, ContinuationController, CSV writers untouched. **P1-I: two new registered heavy cases in `terminal_solver_gpu_cases.cu`.** Case `terminal_eta_endgame` (S1): host-loop eta walk (NO controller changes), 32^3 dx=1 seed 12345 corr 8 normalize_variance amplitude 0.5125*Y_unit K=exp log_conductivity_y SF-19 affine qbar=(1,0,0), epsilon=1e-2 fixed, anderson R5, newton disabled, per-stage `picard.max_iter=500`, warm-started FIXED prespecified ladder: coarse {0, 0.25, 0.5, 0.75, 0.9, 0.95, 0.975, 0.9875, 0.99375, 0.996875} then endgame halvings of `1-eta` from 0.003125 down to 1e-5 ({0.9984375, 0.99921875, 0.999609375, 0.9998046875, 0.99990234375, 0.999951171875, 0.9999755859375, 0.99998779296875, 0.999993896484375}), stop walk at first stage failing r_F<=1e-6 (record exit/r_F); coda per arm: eta=1 attempt (max_iter 500) from (i) last accepted state and (ii) two-point extrapolated init `u* = u_last + ((1-eta_last)/(eta_last-eta_prev))*(u_last-u_prev)` when >=2 accepted stages with eta>=0.99 (record r_F at init too). TWO arms: S1a hygiene OFF; S1b floor_guard+anderson restart ON (prespec values). Readout rules: FRONTIER_EXTENDED (accepted eta>0.996875), CLIFF_CLIMBABLE (accepted eta>=1-1e-4), BASIN_ENTERED (any eta=1 coda converges <=1e-6), ETA_FOLD_SUGGESTED (walk stops at eta_fail<1-1e-4 with plateau signature r_F>1e-5); always record the full r_F(eta) table; wall cap 2h/arm; always-pass evidence recorder. Case `terminal_shelf_probe_phase1` (S3micro+S6, ONE shared E2 freeze): deterministic E2 freeze with the E2 hard band assert [1e-4,1e-2] + printed warn-only exact compare to r_F_frozen=1.1204722529922055e-3. S3micro Part A: single-step mu-scan {0.3,0.4,0.5,0.7,1.0,2.0}, fresh from frozen state per mu, shifted GMRES restart 10 budget 100 rel_tol=clamp(sqrt(r_F),1e-8,1e-1), record inners/status/delta_rF. Part B: mu_best=argmax single-step descent (tie -> smaller mu), N=100 consecutive unconditional full steps (rel_tol recomputed per step from r_F_k), abort iff nonfinite or r_F>2x frozen; record full trajectory+inners+wall. Readouts: rho=(ln r_F_0 - ln r_F_N)/N; PROJECTED_FEASIBLE iff ln(r_F_frozen/1e-6)/rho <= 1e6 steps AND projected wall <= 4h; CURVATURE_LIMITED iff best single-step descent >= 3x sustained mean; FLOW_STALLS iff rho<=0 or last-20 mean <= 0.1x first-20 mean. S6 battery on BOTH the frozen shelf state AND the lambda=0.5 accepted predecessor (baseline column): SF-11 `PhysicalDiagnosticsReport` verbatim + percentiles {50,90,99,99.9,max} of pointwise invariance defect and reconstruction error using the SAME discrete definitions as SF-11 (reuse internals or exact replication), + concentration (fraction of total squared defect in top 1% / 0.1% cells), + |grad psi1 x grad psi2| min/percentiles vs epsilon*scale; rubric (evidence-only): LOCALIZED_OBSTRUCTION iff shelf top-1% >= 50% of defect energy AND concentration >= 5x baseline, else DIFFUSE. **Orchestrator session:** full ctest suite + the two probe jobs on V100; no other runs; no mid-run tuning anywhere. Out of scope: S3-full policy code, S4, S5, YAML loader fields. | PHASE1_PRESPECIFIED |
| 2026-08-14T21:30Z | **PHASE-1 RUN COMPLETE** (V100 job `sf25-phase1`, chain `...->70e0ad6`): all four prespecified readouts obtained; the Phase-2 gate fires NO-GO; two structural findings | **Regression/venue:** build OK; ctest 17/19 PASS — the 2 fails are the PRE-EXISTING known-red gates (`streamfunction_heterogeneity_smoke` = unresolved SF-26 Gate A sigma^2=1; `streamfunction_terminal_dgate` = the documented D-gate FAIL verdict), no regression; all three new P1-H hygiene tests PASS on V100. **S1 eta-endgame:** stages 0/0.25/0.5/0.75/0.9/0.95/0.975/0.9875/0.99375/0.996875/0.9984375 ALL converged <=1e-6 (iterations 0/9/11/18/29/38/49/69/132/128/184; cost roughly doubling per halving of 1-eta; anderson rejections rising 0->47); stage 0.99921875 DIED `omega_floor_rejected` at k=9 (S1a) / k=10 (S1b) at r_F=3.9771052901899963e-4 ~= its own warm-start initial residual — NO descent achieved at all; arms bitwise identical through the frontier (hygiene proven inert until firing); codas from the 0.9984375 state: eta=1 direct -> floor at 8.0442515308299289e-4 (8 its); two-point extrapolated init r_F=1.2040199590271799e-3 -> floor at 1.1980122528535994e-3 (2 its; extrapolation did NOT help). Mechanical verdicts: FRONTIER_EXTENDED=true, CLIFF_CLIMBABLE=false, BASIN_ENTERED=false, ETA_FOLD_SUGGESTED=true. **The reachability boundary is BRACKETED: (1-eta)* in [7.8125e-4, 1.5625e-3) at amplitude 0.5125, 32^3, eps=1e-2.** **S3micro:** freeze warn-compare MATCH exact (r_F_frozen=1.1204722529922055e-3, fourth bitwise reproduction). Part A single-step: mu {0.3,0.4,0.5,0.7} ASCEND (+7.83e-7/+3.70e-7/+2.43e-7/+4.51e-8), mu=1 -3.583579508180737e-8 (bitwise = E6b), mu=2 -5.7240398002207649e-8 => mu_best=2, descent onset in (0.7,1). Part B sustained (100 steps, 0.2456 s/step, inner=1 every step): k=0 descends, then MONOTONE ASCENT from k=1 with GROWING per-step deltas (+1.16e-8 -> +8.29e-6; r_F 1.1204e-3 -> 1.4724e-3, +31%, amplification accelerating — the signature of an unstable eigendirection of the damped map, cf. E4 lambda_min^gen=-2.059e-3). rho=-2.731e-3 < 0 => **FLOW_STALLS; PROJECTED_FEASIBLE=false => the prespecified Phase-2 gate fires NO-GO: S3-full (fixed/servoed-mu massive-cheap-step flow) is REFUTED by its own microprobe before any policy code was written.** **S6:** shelf (r_F=1.12e-3) vs lambda=0.5 CONVERGED baseline (r_F<=1e-6): e_v 2.6425e-2 vs 2.5250e-2; invariance_e psi1/psi2 1.841e-2/2.723e-2 vs 1.755e-2/2.604e-2; e_div 4.20e-3 vs 4.00e-3; percentile tables near-identical; concentration top1% 0.120/0.172/0.096 vs 0.118/0.169/0.095 (ratios 1.017-1.018); abs_c min 0.3535 vs 0.3629 — 33x ABOVE epsilon*scale=1.0756e-2, c_max~2.27. Rubric: **DIFFUSE** (both defect fields; LOCALIZED_OBSTRUCTION refuted at this amplitude). **Unplanned headline finding F-SAT: the r_F=1.1e-3 shelf iterate and an r_F<=1e-6 converged state are PHYSICALLY INDISTINGUISHABLE at 32^3 — the physical defect floor (~2.5% e_v, ~2% invariance) is truncation-dominated; algebraic convergence 1e-3 -> 1e-6 buys no measurable physical quality at this grid.** **S2 hygiene evidence:** (a) rescue omega reset — ctest ON/OFF control green; (c) anderson restart — ctest green (never fired in anger: no stagnated exits in the probes); (b) floor guard — `terminal_floor_guard_continuation` verdict FAIL, ANALYZED: the guard-enabled run reproduced R1a BITWISE to the k=21 death (r_F=0.0090754725182090497 exact, resets=0) => wiring correct, condition GENUINELY unmet — R1a's trailing 5-accepted-iteration drop was <10% at death (the "mid-descent" characterization was true on average, FALSE in the tail); the ON-path firing IS proven at probe level by S1b (resets=1 at the failing eta stage, retry ladder rejected identically) per the prespecified fallback. **Consequence (premise revision by measurement): the R1/R2 64^3 deaths were (at least partly) GENUINE plateau deaths, not machinery artifacts.** **SYNTHESIS:** the three probes triangulate: between eta=0.9984375 and 0.99921875 the discrete system crosses a boundary where the warm-started stage loses ALL local contractivity; at eta=1 the shelf carries a measured unstable direction for every damped/shifted iteration map in our family; and the crossing has NO physical signature (diffuse defects, no gradient degeneracy, physically saturated states) => an ALGEBRAIC/SPECTRAL pathology of the discrete nonlinear system at this grid/amplitude, not a topological/physical event. Flow-stability consequence (recorded reasoning, standard theory): an equilibrium with indefinite Jacobian is unstable for BOTH implicit and explicit pseudo-time flows (Kelley-Keyes assumes an attracting steady state), so the paper's explicit-flow success at 256^3 implies a flow-stable discrete solution THERE => either (i) the 32^3 discretization is spectrally pathological where finer grids are not, or (ii) E4's negative eigenvalue at the SHELF POINT (not the solution) reflects a saddle between basins with a stable solution elsewhere. Discriminating (i)/(ii) is the sharpest remaining question. STATUS: Phase 1 CLOSED per protocol; escalated with options **(P2-A)** long explicit-flow attractor probe (paper-parity control retained in S3's spec: du/dtau=-F, adaptive dt, 1e5-1e6 cheap steps from frozen AND harmonic inits — does ANY flow-stable solution exist at 32^3 eta=1? instrument-only, ~1-3h), **(P2-B)** pseudo-arclength/bordered continuation in ETA around the measured bracket (fold-with-turn-back vs unstable-branch; medium), **(P2-C)** rerun the eta-endgame instrument verbatim at 64^3 (does the (1-eta)* bracket move with h?; cheap, bounded), **(P2-D)** owner-level consumption-policy discussion powered by F-SAT (what "accepted" means at a grid whose physical floor is 2.5%). No runs without owner selection. | AWAIT OWNER PHASE-2 SELECTION |
| 2026-08-15T00:20Z | OWNER DECISION: **Phase 2 = P2-A + P2-C approved.** PRESPECIFIED protocol recorded HERE before any implementation or run | **DAG:** P2-I (worker: both probe cases in `terminal_solver_gpu_cases.cu`, compile-gate only) -> orchestrator V100 session. No production sources touched => no regression surface; the run is build + the two cases only. **P2-A case `terminal_explicit_flow_probe` (32^3, the paper-parity explicit pseudo-time attractor probe):** flow u <- u - dtau*F(u) (forward Euler on the coupled projected residual; 1 residual eval + 2 axpy per step; r_F from the same eval). TWO arms sharing one E2 freeze: **armA1** init = the frozen shelf state (lambda=0.5125, eta=1, r_F_frozen~=1.1205e-3); **armA2** init = zero_source fluctuations (u=0) on the same eta=1 problem (E8-style start, expected initial r_F ~= 0.2525). Step-size policy (accuracy/stability servo, NOT descent-forcing — following the true flow is the point): dtau_0=1e-3, dtau_min=1e-8, dtau_max=1.0; REJECT a step iff r_F_trial nonfinite OR r_F_trial > 2*r_F_current (restore state, dtau/=2, retry; dtau<dtau_min => ABORT_DTAU_FLOOR); ACCEPT otherwise INCLUDING growing steps <=2x (sustained growth is data); after 50 consecutive accepts since the last rejection: dtau <- min(1.5*dtau, dtau_max), streak resets. Budget per arm: N_max=1e6 steps OR wall cap 2.5h (checked every 1000 steps; wall used ONLY for abort, never control — endpoint nondeterminism under the cap recorded as a caveat; the trajectory itself is deterministic given step count). Recording: one line per 1000 steps (k, tau, dtau, r_F, rejections_so_far); every r_F decade crossing printed; argmin-r_F state SNAPSHOTTED per arm (preallocated pair) with its SF-11 `PhysicalDiagnosticsReport` printed at the end; rejection events counted. Step-refinement CONTROL coda per arm: from the final state, 1000 steps at dtau_final/10; compare mean d(ln r_F)/dtau (per unit tau) of the coda vs the last 10000-step segment; print NUMERICAL_GROWTH_SUSPECT iff the sign flips or magnitude changes >2x. Mechanical readouts (multiple may hold, print all): **ATTRACTOR_FOUND** iff min r_F <= 1e-6 (then +100 verification steps must stay <=2e-6 => VERIFIED suffix); **SADDLE_ESCAPE_CONFIRMED** iff armA1 r_F exceeded 2*r_F_frozen at some step AND later achieved min r_F < 1e-4; **NO_STABLE_SOLUTION_AT_HORIZON** iff BOTH arms have min r_F > 1e-5 AND final-10k mean d(ln r_F)/dtau >= 0; **HORIZON_BOUND_DESCENDING** iff min r_F > 1e-5 but final-10k mean d(ln r_F)/dtau < 0 (informative: extend budget later); **BAND_WANDERING** iff >=50% of recorded samples lie in [1e-4,1e-2] without ATTRACTOR_FOUND. Always-pass evidence recorder except the E2 freeze hard asserts. **P2-C case `terminal_eta_endgame_64`:** the S1 eta-endgame instrument at n=64, ell=16, dx=1 (ell/h=16, L/ell=4 — the SAME dimensionless problem as the 32^3/ell=8 case with h halved; SF-18 hash-mode realization caveat REPRINTED: statistically equivalent field, not the same continuum realization). Identical 19-value ladder, identical stage acceptance (converged AND r_F<=1e-6), identical per-stage budget (max_iter=500 — comparability; record iteration_budget vs floor deaths distinctly), identical codas from the last accepted state; ONE arm only, hygiene ON with the S1b config (floor_guard{5,0.9,3} + anderson restart{2}; rationale: arms were bitwise-identical through the frontier at 32^3, hygiene proven inert-until-firing; 64^3 is ~8-10x costlier). Implementation: parameterize the existing arm runner by (n, ell) passing EXACTLY the previous constants at the 32^3 call sites (32^3 behavior must remain identical; duplication acceptable if transparent parameterization is not); wall cap 5h. Mechanical comparison vs the 32^3 bracket [7.8125e-4, 1.5625e-3): **BRACKET_MOVED_TOWARD_1** iff last accepted eta > 0.9984375; **BRACKET_SAME** iff last accepted eta == 0.9984375; **BRACKET_MOVED_AWAY** iff last accepted eta < 0.9984375. Always-pass evidence recorder. **Interpretation frame (recorded pre-run):** P2-A discriminates hypothesis (i) no flow-stable 32^3 solution (NO_STABLE_SOLUTION_AT_HORIZON) vs (ii) saddle-between-basins with a stable solution elsewhere (ATTRACTOR_FOUND / SADDLE_ESCAPE_CONFIRMED); P2-C measures the h-dependence of the reachability boundary on the second axis. No mid-run tuning; readout rules fixed HERE. | PHASE2_PRESPECIFIED |
| 2026-08-15T17:20Z | **PHASE-2 RUN** (V100 job `sf25-phase2`, chain `...->0029d7f`): P2-C verdict CLEAN and inverted-direction; P2-A mechanically fired but CONFOUNDED by an identified instrument artifact; corrective amendment PRESPECIFIED here before the rerun | **P2-C (`terminal_eta_endgame_64`, implicit stack, NOT confounded): BRACKET_MOVED_AWAY.** Stages 0/0.25/0.5/0.75/0.9/0.95 converged <=1e-6 (its 0/9/11/18/35/112; the 0.95 stage cost 112 its vs 38 at 32^3 — pre-frontier stiffening ~3x); stage 0.975 DIED `omega_floor_rejected` at k=12, r_F=3.9158e-3 (early-collapse signature, guard did not fire); frontier_eta=0.95; coda eta=1 -> floor at 1.0186e-2; extrapolation skipped (<2 accepted stages >=0.99). **(1-eta)*_64 in [0.025, 0.05) vs (1-eta)*_32 in [7.8125e-4, 1.5625e-3): the reachability boundary moved ~30x AWAY from eta=1 under h-refinement** (SF-18 realization caveat reprinted and standing). This REFUTES the "coarse-grid-only spectral pathology" version of hypothesis (i): refinement WORSENS damped-map reachability — consistent with R2's WALL_AT_ELLH16; sharpens the discretization-vs-paper comparison question. **P2-A (`terminal_explicit_flow_probe`): raw results retained; mechanical NO_STABLE_SOLUTION_AT_HORIZON=true VACATED by confound analysis.** armA1 (frozen init 1.1204722529922055e-3): descended to min_r_F=1.061034626344017e-3 (-5.3%) at k=420/tau=2.9755, then sawtooth/blow-up to 5.84e16 by k=891, ABORT_DTAU_FLOOR (dtau 7.34e-9), 27 rejections, wall 0.413 s; argmin SF-11 ~= the shelf's S6 numbers (e_v=2.6426e-2). armA2 (zero-fluctuation init, measured init r_F=3.1755269835780879): oscillated (10 <-> 0.87-1.3), min_r_F=0.47786 at k=741/tau=12.76, blow-up to 8.25e6 by k=121324, ABORT_DTAU_FLOOR, 1292 rejections (~1 per 94 steps — the sawtooth cadence), wall 54.77 s (0.45 ms/step — rolling-residual structure confirmed). BOTH control codas SKIPPED (main runs died at the dtau floor) => NUMERICAL_GROWTH_SUSPECT unevaluable => the joint rule is VACUOUS as evidence. **Confound reconstruction (recorded):** the dtau servo (x1.5 per 50 accepts, cap 1.0) crosses the explicit-Euler stability boundary: armA1's minimum at k=420 coincides with dtau ~= 1e-3*1.5^8 ~= 0.026 ~= the 2/lambda_max estimate; beyond it the stiffest mode compounds a few %/step — invisible to the 2x-per-step rejection guard — producing decade-by-decade growth and the observed sawtooth (armA2 rejection cadence ~50-100 steps = the re-growth period). The probe measured the SERVO, not the flow, beyond each arm's turning point. **Faithful-segment measurement (armA1, k<=420, dtau <= ~0.017): d(ln r_F)/dtau ~= -1.8e-2 per unit tau — the true explicit flow DESCENDS from the shelf.** Projection: reaching 1e-6 needs Delta-ln ~= 6.97 => tau ~= 380 => ~76k steps at dtau=0.005 ~= 35 s wall at the measured 0.45 ms/step. **PRESPECIFIED AMENDMENT P2-A' (measurement-methodology correction, instrument-only, recorded BEFORE the corrective run):** rerun `terminal_explicit_flow_probe` with the SINGLE change kDtauMax: 1.0 -> 0.005 (>=3x below the empirical destabilization threshold ~0.026 and ~5x below the 2/lambda_max estimate; growth schedule and every other rule/budget/readout UNCHANGED; raw first-run numbers retained above). Decisive either way: capped-flow reach of r_F<=1e-6 => ATTRACTOR_FOUND (VERIFIED) => the paper-class explicit flow IS the terminal method AND we hold the 32^3 solution; stall/divergence at stable dtau => NO_STABLE_SOLUTION_AT_HORIZON with real teeth. Corrective node P2-I-C1 (worker, one-constant amendment + case doc-string update) -> audit -> V100 rerun of the one case. | PHASE2_AMENDED_RERUN_PENDING |
| 2026-08-15T18:10Z | **P2-A' RERUN** (V100 job `sf25-p2aprime`, chain `...->a80de8d`): both arms ran the FULL 1e6-step budget at the stability-capped dtau; no attractor found; the shelf is flow-repelling; the reachable floor converges across method families; residual instrument contamination self-reported by the control codas — Phase 2 CLOSED | **armA1 (frozen shelf init):** min_r_F=1.0610460597828266e-3 (-5.3%) at k=717/tau=2.99, then the flow ESCAPES the shelf upward — decade crossings 1e-2 at tau=95.3, 1e-1 at tau=102.5, O(1) at tau=103.1 — and wanders an O(1)-O(10) band with single-step decade flips; final r_F=8.94 at tau=4963, k=1e6, 138 rejections, wall 430.7 s, bound=N_max; final-10k slope +0.0067. Control coda (dtau/10): 8.94 -> 5.61, slope -0.933 => **NUMERICAL_GROWTH_SUSPECT=true** (sign flip: the O(1)+ band dynamics are partly step-size artifact — local stiffness grows with |u|, so the fixed dtau=0.005 that is stable near the shelf is unstable out in the band). **armA2 (zero-fluctuation init, r_F0=3.1755):** spent >half the horizon in the [1e-4,1e-2] band (**BAND_WANDERING=true, 564/1000 samples**), reached **min_r_F=4.0543494398799805e-4 at k=493565/tau=2467**, periodic excursions to O(10); final r_F=19.6 with final-10k slope -0.0087 => **HORIZON_BOUND_DESCENDING=true**; coda slope +0.367 => NUMERICAL_GROWTH_SUSPECT=true (opposite flip). argmin SF-11 of BOTH arms ~= the shelf's S6 profile (A2 argmin: e_v=2.6384e-2, invariance 1.838e-2/2.722e-2, c_min 0.353 — F-SAT reconfirmed from an independent trajectory: every state in this region is physically saturated at the 32^3 truncation floor). Mechanical: ATTRACTOR_FOUND=false both; SADDLE_ESCAPE_CONFIRMED=false; NO_STABLE_SOLUTION_AT_HORIZON=false (A2's end-descent blocks it). **Quantitative convergence of evidence: the flow family's min (4.05e-4) matches the implicit family's failing-stage death level (3.98e-4 at the eta-walk's 0.99921875 stage) — the repelling shell at 32^3/eta=1 sits at r_F ~= 4e-4, approached independently from two method families.** **PHASE-2 SYNTHESIS (closed):** (a) implicit family: unreachable past (1-eta)* ~= 1e-3 at 32^3 and ~3e-2 at 64^3 (30x WORSE under refinement); (b) explicit flow family: the shelf is flow-repelling; no attractor <=1e-6 found over tau ~= 5000 from either init; reachable floor ~= 4e-4; the O(1)+ excursion dynamics remain contaminated by state-dependent-stiffness step-size effects (self-reported by the prespecified control codas — the fixed-cap integrator cannot faithfully integrate the large-|u| region); (c) F-SAT: all states in play are physically indistinguishable at 32^3. The attractor-existence question is NARROWED but remains open pending an error-controlled integrator; the h-trend question now has two clean data points pointing the WRONG way for a pure-resolution explanation, sharpening the formulation-vs-paper comparison (their sec. 5.1: explicit variable-step — i.e., error-controlled — pseudo-time, no epsilon regularization, 256^3). Note: epsilon is NOT shaping the local landscape here — |grad psi1 x grad psi2| min is 33x above epsilon*scale (measured, S6). STATUS: escalated with options **(P3-A)** amendment P2-A'': error-controlled explicit integrator (embedded Heun/Euler pair, tolerance-servoed dtau — the paper's actual "variable-step" class; ~40 instrument lines + rerun; the last fidelity step on the attractor question), **(P3-B)** formulation/discretization comparison vs Lester sec. 5.1 (research + decision record: spectral-vs-FD, total-psi vs affine split, no-epsilon; then the closest-to-paper probe our code can express), **(P3-C)** S5 bordered fold analysis in eta with the two measured brackets (settles fold-vs-repelling-branch mathematically), **(P3-D)** close SF-25 as delivered-diagnostics + BLOCKED-on-terminal-method and re-scope. No runs without owner selection. | AWAIT OWNER PHASE-3 SELECTION |
